{% extends 'base.html.twig' %}

{% block body %}
    <h1>Product creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-success pull-left"/>
    {{ form_end(form) }}

    <a class="btn btn-default pull-right" href="{{ path('product_index') }}">Back to the list</a>
{% endblock %}